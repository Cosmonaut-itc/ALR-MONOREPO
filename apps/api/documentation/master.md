# master

- Modularized `apps/api` routes into domain routers under `src/routes/auth`.
- Added `packages/api-contract` for shared RPC contract types.
- Updated web and mobile clients to consume `@ns-inventory/api-contract`.
- Added `eslint.config.mjs` to align linting with ESLint v9 flat config.
